using Krimson.Examples.Messages.Telemetry;

// WebApplication
//     .CreateBuilder(args)
//     .UseKrimson<PowerConsumption>((msg, ctx) => ctx.Logger.LogInformation("{PowerConsumption}", msg))
//     .Build()
//     .Run();

// builder.Host.UseSerilog((context, services, logger) => logger.ReadFrom.Configuration(context.Configuration));